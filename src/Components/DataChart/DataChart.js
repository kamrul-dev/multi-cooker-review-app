import React from 'react';

const DataChart = ({dataChart}) => {
    const {month, investment, sell, revenue} = dataChart;
    console.log(month)
    return (
        <div>
            
        </div>
    );
};

export default DataChart;